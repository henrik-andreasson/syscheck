# config for related-available/900_export_cert.sh

### config ###

# If you configure one or more REMOTE_HOST:s the archived certificate will also be stored on that host
REMOTE_HOST[0]="127.0.0.1"
REMOTE_USER[0]='han'
REMOTE_CMD[0]='echo'
# can be one of SN, UID, CN , CERTSERIAL, DN and CERTSTRING
REMOTE_ARG[0]='SN'
SSHKEY[0]=''

REMOTE_HOST[1]="127.0.0.1"
REMOTE_USER[1]='han'
REMOTE_CMD[1]='echo'
# can be one of SN, UID, CN , CERTSERIAL, DN and CERTSTRING
REMOTE_ARG[1]='UID'
SSHKEY[1]=''

REMOTE_HOST[2]="127.0.0.1"
REMOTE_USER[2]='han'
REMOTE_CMD[2]='echo'
# can be one of SN, UID, CN , CERTSERIAL, DN and CERTSTRING
REMOTE_ARG[2]='CN'
SSHKEY[2]=''

REMOTE_HOST[3]="127.0.0.1"
REMOTE_USER[3]='han'
REMOTE_CMD[3]='echo'
# can be one of SN, UID, CN , CERTSERIAL, DN and CERTSTRING
REMOTE_ARG[3]='CERTSERIAL'
SSHKEY[3]=''

REMOTE_HOST[4]="127.0.0.1"
REMOTE_USER[4]='han'
REMOTE_CMD[4]='echo'
# can be one of SN, UID, CN , CERTSERIAL, DN and CERTSTRING
REMOTE_ARG[4]='DN'
SSHKEY[4]=''

REMOTE_HOST[5]="127.0.0.1"
REMOTE_USER[5]='han'
REMOTE_CMD[5]='echo'
# can be one of SN, UID, CN , CERTSERIAL, DN and CERTSTRING
REMOTE_ARG[5]='CERTSTRING'
SSHKEY[5]=''

### end config ###
