# config for 928

### config ###

# 
REMOTE_USER='username'
REMOTE_DIR='/monitoring/'
SSHKEY='/home/username/.ssh/id_rsa'
REMOTE_HOSTNAME="external.server.tld"

# If you configure one or more REMOTE_HOST:s the archived certificate will also be stored on that host
LOCAL_FILE[0]="${SYSCHECK_HOME}/var/last_status_filtered"
REMOTE_FILE[0]='hostname.log'
